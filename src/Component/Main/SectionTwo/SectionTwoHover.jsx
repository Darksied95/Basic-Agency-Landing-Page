import React from "react";
import Hover from "./../../Hover/Hover";

const SectionTwoHover = ({ ref, showHover, hideHover }) => {
  return <Hover>SectionTwoHover</Hover>;
};

export default SectionTwoHover;
